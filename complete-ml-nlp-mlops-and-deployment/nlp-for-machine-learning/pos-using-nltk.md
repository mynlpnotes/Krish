# POS using NLTK

*

    ```python
    nltk.pos_tag(words)
    # [('I', 'PRP'), ('three', 'CD'), ('visions', 'NNS'), ('India', 'NNP'), 
    # ('.', '.')]

    print(nltk.pos_tag("Taj Mahal is a beautiful Monument".split()))
    ```
