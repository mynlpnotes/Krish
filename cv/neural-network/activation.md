# Activation

* So activation is basically how +ve the weighted sum is
* But its not always that we want the neuronI to be active when weighted sum > 0

Q: I want my neuron to be active when sum > 15, then we need to add bias -15

* So weight is responsible for pixel pattern, a particular neuron is picking and bias tells you the sum of weighs needs to be before the neuron start to getting meaningfully active
