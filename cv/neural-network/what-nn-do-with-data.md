# What NN do with data?

Compression:

* Each layer filters out irrelevant details and reduce the inputs dimensionality&#x20;
* In image data, these layers identify edges, corners and textures the complexity of raw pixel data
* Deeper layer combine simpler features to form high level representation -> for edges to shapes to objects
* Ability to generalize
