# LLM

* A language model predicts the probability of a sequence of words
* Transformers can process sequence of tokens in parallel using self attention mechanism
* Because of this transformers can capture long term context more easily
* The cost of self attention is that it limits the length of the context
* RNNs have infinite context length but suffer from vanishing gradient etc
