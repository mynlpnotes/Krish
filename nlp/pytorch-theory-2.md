# PyTorch Theory - 2

<figure><img src="../.gitbook/assets/image (1) (1) (1) (1) (1) (1).png" alt=""><figcaption></figcaption></figure>

* Tensor is special array, to compute on GPU
* Tensor can also calculate gradient
* NN used multiplicated of weights and biases

Scalar: 0 dimension array

1D array: \[4,2,3] ⇒ vector

* Similarly tensor also we can create 1D, 2D array etc.,
* Tensor is specialized array for training DL models
* For training huge data we need parallel processing and graphical processing so tensor was use
