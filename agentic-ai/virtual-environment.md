# Virtual Environment

```
conda create -p <<env_name>> python==3.12
conda create -n <<env_name>> python==3.12

conda activate <<env_name>>
```

-n: the location where anaconda is installed

-p: in the current path

pip install -r requirements.txt
