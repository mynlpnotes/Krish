# Prompt - Theory

* With prompt we can ask more innovative questions to LLM
* LLM is trained with huge amount of data, we can make it act as different by using prompt
* System: to provide instruction to LLM
* User: to give user message
* Whenever user asks any question it should go to the prompt and then it should go to LLM
* For this langchain has created chain concept
*
